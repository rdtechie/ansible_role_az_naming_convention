---
- name: Create Name for a Azure Resource Group
  hosts: localhost
  connection: local
  gather_facts: false
  vars_files:
    - ../vars/vars.yml
  roles:
    - { role: ../../ansible_role_az_naming_convention }
...